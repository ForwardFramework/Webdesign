{
  "name": "roughneck-roofing-website",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing website for Roughneck Roofing — Pittsburgh, PA",
  "scripts": {
    "build": "node build.js",
    "check": "node build.js && node scripts/check.js",
    "og": "node scripts/og.js",
    "serve": "node build.js && node scripts/serve.js",
    "start": "npm run serve"
  },
  "engines": { "node": ">=18" }
}

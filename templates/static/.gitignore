# Netlify CLI local state (contains the site ID — regenerated by `netlify link`)
.netlify/

# Dependencies and build output
node_modules/
dist/

# Local environment files
.env
.env.local

# Local Claude Code settings — may hold secrets, never commit
.claude/settings.local.json

# OS cruft
.DS_Store

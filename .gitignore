# Local Claude Code settings — may hold secrets, never commit
.claude/settings.local.json

# Local environment files
.env
.env.local

# Generated single-file bundle — rebuild with `node talent-signal/build.mjs`

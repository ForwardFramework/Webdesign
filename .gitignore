# Local Claude Code settings — may hold secrets, never commit
.claude/settings.local.json

# Local environment files
.env
.env.local

# Dev tooling used for build-time checks only (playwright, axe-core)
node_modules/
package.json
package-lock.json

# Python bytecode from the site generator
site/__pycache__/

# Local Claude Code settings — may hold secrets, never commit
.claude/settings.local.json

# Local environment files
.env
.env.local

# Build output — Netlify regenerates it with `node build.mjs`
dist/

# Local tooling
node_modules/
.shots/
.netlify/

# Local Claude Code settings — may hold secrets, never commit
.claude/settings.local.json

# Local environment files
.env
.env.local

# Node dependencies (playwright-core, installed on demand for tests/e2e.js)
node_modules/
package-lock.json

# Test screenshots — regenerate with `node tests/e2e.js`
tests/shots/
